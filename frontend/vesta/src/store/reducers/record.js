
const initialState = {
    userRecords: null,
    selectedRecord: null,
}
const recordReducer = (state=initialState, action) => {
    switch (action.type) {
        default:
            break;
    }
    return state;
}

export default recordReducer;