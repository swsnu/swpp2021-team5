const initialState = {
  selectedMenu: null,
  allMenus: null,
  recommendedMenus: null,
};
const menuReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      break;
  }
  return state;
};

export default menuReducer;
