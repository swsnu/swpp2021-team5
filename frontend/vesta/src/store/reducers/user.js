const initialState = {
  user: null,
  currentUser: null,
  userNutrition: null,
};
const userReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      break;
  }
  return state;
};

export default userReducer;
